# SDN-Learning-Switch-Controller
This project implements an SDN Learning Switch using the Ryu controller and Mininet. The controller dynamically learns MAC-to-port mappings by handling packet-in events and installs OpenFlow rules to enable efficient packet forwarding, reducing unnecessary flooding and improving network performance.
